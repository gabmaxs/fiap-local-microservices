apiVersion: v1
kind: Service
metadata:
  name: payment-api-service
  namespace: tech-challenge
  labels:
    app: payment-api
spec:
  selector:
    app: payment-api
  type: LoadBalancer
  ports:
    - name: http
      port: 8888
      targetPort: 80
      protocol: TCP